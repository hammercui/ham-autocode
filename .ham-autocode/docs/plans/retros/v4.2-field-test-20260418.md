# v4.2 Routing v2 Field Test — 2026-04-18

> 目标项目: ham-video, Phase 5 (3 纯新文件小任务)
> 变更范围: 路由 v2 + gpt-5.4-mini + cc-sonnet/cc-haiku + ab-log 闭环

## 测试场景

3 个独立单文件工具任务 (complexity=20, isolation=100):
- task-010: `app/src/renderer/lib/clamp.ts`
- task-011: `app/src/renderer/lib/chunk.ts`
- task-012: `app/src/renderer/hooks/usePrevious.ts`

## Run 结果

```
Phase 5 full-auto:
  task-010 ✓ codexfake 32s, 56,771 tokens  (L4 review PASS)
  task-011 ✓ codexfake 48s, 16,710 tokens  (L4 review PASS)
  task-012 ✓ codexfake 52s, 52,588 tokens  (L4 review PASS)

总耗时: 108s
Commit: 8569db3
3/3 ok, 0 fallback, 0 skip
```

## 路由决策分析

全部命中 **R1** (`isolation ≥ 80 AND complexity ≤ 50 → codexfake`)。
- 符合预期: 3 个独立单文件任务天然高 isolation
- 验证: gpt-5.4-mini 通过 opencode CLI 工作正常
- 验证: L4 review 链路未受路由变更影响

## 直接验证的部件

| 部件 | 状态 | 证据 |
|------|------|------|
| gpt-5.4-mini 模型切换 | ✅ | 3/3 codexfake 成功，token 解析正确 |
| R1 决策链 | ✅ | 3/3 R1 命中日志 |
| auto-runner shellCmd 分支 | ✅ | 新 cc-sonnet/cc-haiku 分支存在不影响 codexfake 路径 |
| parseAgentOutput 分流 | ✅ | token 数值 (56771 / 16710 / 52588) 与 opencode JSONL 对齐 |
| L4 review | ✅ | 3/3 PASS |

## 未覆盖部件 (需后续实战)

| 部件 | 原因 | 补验方案 |
|------|------|---------|
| R2 随机档 (opencode vs cc-haiku) | 3 任务 iso=100，R1 截胡未到 R2 | 自然项目中有共享文件的任务会触发 |
| ab-log.jsonl 回填 | R2 未触发 → 无记录 | 同上 |
| cc-sonnet 路径 | 需 complexity 60-75 的任务 | 后续多文件任务自然触发 |
| fallback chain (cc-haiku→opencode) | 首选即成功 | 人工制造失败场景或实战中遇到 |
| `claude -p` 端到端 | **直接 shell 测试已验证** (26s, JSON 正确) | 已间接验证 |

## 结论

v4.2 路由 v2 **核心路径 ship-ready**。
- 现有 R1 路径无回归
- 新增 R2/R4/cc-sonnet/cc-haiku 路径代码走查 + 单测通过 + 解析器对真实 claude -p 输出验证
- 实战覆盖将在后续自然使用中渐进完成

## 产出文件

- ham-video: commit `8569db3` — 3 个新工具文件
- ham-autocode: commits `a239c6f` (路由 v2) + `b80b213` (claude-sub 解析器)
